/**
 * at first go and creat a file in console.firebase.google.com;
 * npm install firebase;
 * create firebase.init.js and import getAuth to export auth;
 * firebase settings > Authentication > enable email and password auth
 * create log in, sign up component and setup route
 * attach form field handler and form submit handler
 * npm install --save react-firebase-hooks
 * useCreateUserWithEmailAndPassword from, react fireBase Hooks
 *  
 **/





/**
 * hosting steps
 * 1: npm install -g firebase-tools
 * 2: firebase login
 * 3: firebase init
 * 4: firebase deploy
 * 
 * **/ 